@model IEnumerable<VibeLang.Models.Chapter>

@{
    ViewData["Title"] = "Manage Chapters";
}

<h1 class="h4">Chapters</h1>

<p>
    <a asp-action="Create" class="btn btn-primary rounded-pill">Add New Chapter</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>Course</th>
            <th>Title</th>
            <th>Order</th>
            <th>Actions</th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>@item.Course?.Title</td>
            <td>@item.Title</td>
            <td>@item.Order</td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.Id">Edit</a> |
                <a asp-action="Delete" asp-route-id="@item.Id">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
